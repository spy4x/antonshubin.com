
.age/
# Age encryption key (never commit!)
anton-tasks.md
data/subscribers.json
.env
# Environment files
.env.*.local
.env.prod
.env.age
.env.*.local.age
# dev .env's own encrypted form was never tracked before age64 either; only
# .env.prod.age is committed. deploy:stag writes .env.staging.local, so
# env:encrypt would turn it into a committable .env.staging.local.age too.
_fresh/
# Fresh build directory
launches/
# Generated launch-kit drafts
videos/
# Generated video-kit drafts
key.txt
# Local private files
node_modules/
# npm + other dependencies
.playwright-mcp/
todos.md
vendor/
docs/seo-optimization-roadmap/
