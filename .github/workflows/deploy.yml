# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy
'on':
  push:
    branches:
      - main
      - test-deploy
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install --frozen-lockfile --non-interactive
#      - uses: FirebaseExtended/action-hosting-deploy@v0
#        with:
#          repoToken: '${{ secrets.GITHUB_TOKEN }}'
#          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ANTONSHUBIN_PRODUCTION }}'
#          channelId: live
#          projectId: antonshubin-production
#      - name: Use Node.js 16
#        uses: actions/setup-node@v1
#        with:
#          node-version: 16
#      - name: install libraries
#        run: yarn install
#        working-directory: ${{env.working-directory}}
#      - name: install firebase
#        run: npm i firebase-tools -g
      - name: deploy
        run: yarn firebase use --add ${{secrets.FIREBASE_PROJECT}} && yarn firebase deploy --project ${{secrets.FIREBASE_PROJECT}} --token ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ANTONSHUBIN_PRODUCTION }}
